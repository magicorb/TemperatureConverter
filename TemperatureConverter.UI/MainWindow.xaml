<Window x:Class="TemperatureConverter.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TemperatureConverter.UI"
		xmlns:properties="clr-namespace:UnitConverter.UI.Properties"
        Title="{x:Static properties:Resources.MainWindowTitle}"
		Width="400" Height="200">
	<Window.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="Common.xaml"/>
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</Window.Resources>
	
    <Grid
		HorizontalAlignment="Center" VerticalAlignment="Center">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition Width="100"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		
		<Button
			Grid.Row="0" Grid.Column="0">
			Switch
		</Button>
		
		<TextBox
			Grid.Row="0" Grid.Column="1"/>
		
		<Button
			Grid.Row="1" Grid.Column="0">
			Calculate
		</Button>
		
		<TextBox
			Grid.Row="1" Grid.Column="1"
			Style="{StaticResource OutputTextBoxStyle}"/>
	</Grid>
</Window>
